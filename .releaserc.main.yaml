# Semantic-release extended configuration for main release channel of this Obsidian plugin.
# Run `npx semantic-release` in CI after building the selected release variant.
plugins:
  - - "@semantic-release/git"
    - assets:
        - manifest.json
      message: >-
        chore: :rocket: deploy release ${nextRelease.version} [skip ci]

        ${nextRelease.notes}
